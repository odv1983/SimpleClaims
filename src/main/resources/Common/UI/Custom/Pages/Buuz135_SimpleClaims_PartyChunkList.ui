$C = "../Common.ui";

$C.@PageOverlay {

  $C.@DecoratedContainer {
    Anchor: (Width: 700, Height: 669);

    #Title {

      TextButton #BackButton {
        Anchor: (Left: 12, Top: 4, Width: 64, Height: 24);
        Padding: (Left: 8, Top: 2);
        Text: %ui.simpleclaims.party.back;
        Style: TextButtonStyle(
          Default: (LabelStyle: (FontSize: 14, TextColor: #93844c, RenderBold: true)),
          Sounds: $C.@ButtonSounds,
        );
        Background: #000000(0.13);
      }

      Group {
        $C.@Title #TitleText{
          @Text = %ui.simpleclaims.party.claimedChunksTitle;
        }

      }
    }

    #Content {
      LayoutMode: Left;

      Group #Main {
        LayoutMode: Top;

        
        Group #ClaimList {
          Anchor: (Width: 680);
          FlexWeight: 1;
          LayoutMode: TopScrolling;
          Padding: (Left: 10);
          ScrollbarStyle: $C.@DefaultScrollbarStyle;

          Group #ClaimsCards {
            LayoutMode: Top;
          }
        }
      }
    }
  }
}

$C.@BackButton {}